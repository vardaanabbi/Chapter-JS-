to link js file with html , before ending of body tag use <script src="<file name of js> 

notes available on cod with harry .com

var is global scoped whereas let and const are block scoped . var can be updated and redefined whereaslet can be updated only and const dono hi nhi . 
semicolon is used to end every statement declared if you wish to use it . 

let is most commonly used instead of var in js .

there are two data sets in js which are object and data type , 7 primitive data types are string , number , null , undefined , big int , boolian , symbol . but typeof null is object as it was set by mistake in early days and due to reliability of so much of code already written on its basis , it cant be changed now . 
object is like a dictionary which is used to store info about a particular subject . it stores values in form of key - value pair . curly brackets are used while working with objects

o.salary = 69 matlab o object me ek key salary ko 69 value assign krdi . 

== is used to compare the value only , it dont check the data type . if you want to compare both value and data type then use === . 
logical operator , and is && here || is or here , ! stands for not . 

if - else blocks : only if block ; if - else block ; if else-if else block .. ; 
last one is like if elif else in python . here it is else if . 

ternary operator : example let c = a>b ? (a-b) : (b-a) 
translates to : if (a>b) { let c = a-b} else { let c = b-a } , here ?  and : are ternary operator which are kind of short cut for if else condition . 

console.log() works like print works in python . 

loops : for , for in , for of , while , do while , for each loop .

for loop syntax : for (statemnet1 , statemnet2 , statement3) {code to be executed like console,log(a+i)} where statemnet1 is one time condition for initialization of loop , statement 2 is base condition for loop which is checked everytime and statement3 is iteration . 

example : let a = 1 
            for (let i = 0 ; i<100 , i ++) { console.log(a+i)} . 

for in : example : let obj = { name : "vardaan" , role : "tera baap" , contribution : "tujhe krne me sahyog" }
    for (const key in obj) {console.log(key)}

for of : used in iterables like strings or arrays . 
example : for ( const a of "vardaan" ) {console.log(a)} 

while : example : let i = 3 
                    while(i<6) { 
                    console.log(i)
                    i++ }

do-while loop : exampple : 
        let i = 6 
            do {console.log(i)
            i++} 
            while (i<7)
summarizing all the loops : 
for loop is just like for i in range (..) in python . 
for in is used object , dictionary , key-value iterations 
for of is like for i in "  " or a list or array etc iterations . 
while loop is just like python . 

functions : syntax is function <function-name> (parameter) {code to be executed}
example : function wish(name) { "Good Morning " + name}
to assign function value to a variable , you must return the value inside the function first . 
example : sol = wish("vardaan") is not correct but if : function wish(name) { return ("Good Morning " + name) }
then sol = wish("vardaan") is correct . 

types of argument works similar to python . same things default parameter etc . 

arrow function : used when you want to store function in a variable . similar to lamda in python 
example : const func1 = (x) => {console.log("I am a fancy arrow function" , x)}
call : func1(33) etc

strings in JS : indexation is same as in python .  
template literal : vahi formatted string vali cheez ka fancy name hai ye . 
let v = "Vardaan"
example : console.log(`My name is ${v}`) , this special comma is used known as back tick under esc and above tab key . this is used when there are a lot of variables involved .
this process of formatting a string is called string interpolation . 

escape sequence characters : vahi \n se new line , \r se carriagre return , \ to ignore meaning of a specific symbol in string like before double quotes . 

string methods : 
let b = "Skanda"
console.log(b.toUppercase()) , console,log(b.toLowercase()) , console.log(b.length)
() not aftrer length as length is a property and not a function . 

slicing methods : console.log(b,slice(c,d)) where c and d are indexes and d excluded . 
for a particular index : console.log(b[2])
console.log(b.slice(2)) will print all characters from index 2 to end . 
to replace small string : console.log(b.replace("aan" , "ati")) and it will replace first occurence if multiple are there . 

console.log(b.concat(variable , "string" , ...)) can concatenate multiple strings one after other . 
trim() removes whitespaces from the string
.charAt(index) gives character at that index . 
.indexOf("va") gives starting index of that particular string . 
.startsWith("ar") gives boolean value of this . 
.endsWith works similarily . 

* strings are immutable which means you cant change the string once made , you may create another using the same .* 

array : let arr = [1,4,7] . arrays are mutable objects . 
array methods: .toString() converts array elements to a string , .join(" abc ") se comma ki jagah abc aa jayega array me , .pop() se last vala element remove ho jayega
.push(element) se element last me place ho jayega and returns the length of array  .
.shift() se first element remove hoga and .unshift(eleemnt) se 0 index pe element chla jayega .
.concat(a1,a2) will combine these three arrays . delete(arr[3]) removes that element eeping that space as empty . .sort() sorts the array in increasing order . .splice(a,b) se ye array ke b elements return krdega from "a" index and vo elements array se remove ho jayenge . 
.splice(a,b,c,d) aisa krega ki a position se b numbers remove krke c aur d add krdega array me a position pe . .slice(a,b) will return a new array starting from index a to b exluding b .
.reverse() reverse the elements in an array . .forEach() function is used to apply a function on each element of the array . 
example of forEach function use case :
let Divine = ["Skanda" , "Narayana" , "Bhagwati" , "Shiv"]
arr.forEach(function(divinity){console.log("Jai Bhagwaan" , divinity)}) 
apply function to each element of the array . 

in form of arrow function : arr.forEach(element)=>{console.log(element)} 

filter , map , reduce functions :

example : arr = [1,2,3,4,5]
new_arr = arr.map((e)=> {return e**e})

filter is use to filter out by using a function :

example : arr = [1,4,7,9,81]
const greaterThan5 = (f)=> {if(f>5) {return True}
else {return False}}
console.log(arr.filter(greaterThan5))

reduce function is used to reduce an array to a single output .

example : let arr2 = [3,6,9,12]
const reduc = (a,b)=> {return a*b}
console.log(arr2.reduce(reduc))

Array.from(object) this function will create an array from this object passed . 
example : Array.from("Vardaan)

DOM : docment object model stands for the whole document as an object . document is represented as a tree with nodes and object of nodes . BOM is browser object model where browser offer various methods and features to access different objects of html document like $0 se access kr skte hain under inspect . 

Browser contains an embedded engine known as js engine . every browser have some developer tools . F12 se you can access dev tools . element contains elements , network contains network requests and console includes all errors and log . we can write js commands in the console . 
benifit of js file is that browser will download it and stpore it in it's cache . 
console commands : alert() , log() , table() , warn() , error() , info() etc . 

auto correction is BOM where it auto corrects the html file like if you add something after body then browser auto shufts the content inside the body . 

DOM contains various methods and ways to access or target a particular element . 
various methods are ; document.body.style.color : "red" will apply inline css . similarily
document.body.childNodes se you will get nodes : text , div etc in the body . text here is actually the empty space before and after of <>tags . we can target through .childNodes , .firstChild , .lastNode , .firstElementChild se youll get first child which is an element and not text , .nextElementSibling , .previousElementSibling se you can target similar element which is element from the same parent . 
table.rows , table.thead vagera use krke table ke elements ko target kr skte hain . 

Searching the DOM : document.getElementById . example ; let element = document.getElementById("spy1") , spy1.style.bg-color :"black" . similarily document.getElmentById("<id_name>").style.<property> se implement ho jayega .
document.querySelector(".<class_name>") se jo first eleent match hoga is se uspe work krega ye .
if you want to select all those elements then use querySelectorAll instead of querySelector but you can't directly apply styles to all elements , youll hae to use for each loop and iterate them one by one because All returns an array  . you can use any for loop  

console.log(document.getElementsByTagName("div")) se you can access elements by tag name .
other methods : matches ;
let e = document.getElementsByTagName("div") ; e[3].matches("#box") , returns True , false . 
closest ;
e[2].closest(".container")
contains ; 
example : document.querySelector(".conatiner").contains(e[2]) , all of them returns boolean . 

There are many functions to use . .innerHTML se we get HTML of that element , .innerText se we get HTML of that element in text format . example :
document.querySelector(".container").innerText se we get HTML of elements inside that class and if we use .outerHTML or .outerText se we get HTML of that class as well of elements inside that class . .tagNme se tag pta chl jayega aur .nodeName se node ka pta chl jayega . .text se andar ka text mil jayega . .hidden se element hide ho jayega . ex ; document.querySelector(".container").hidden = true se attribute lg jayega .
 similarily we can change the innerHTML of that element too . .hasAttribute("<attribute like style>") returns boolean . .getAttribute("attributeName") se we get whats in that attribute . 
 .setAttribute("style"  , "display : none") se we can change or set attribute . 
 .attributes se we get all the attributes of that element . .removeAttribute se we can remove attribute . document.designMode = "on" se we can change the design content by backspace etc . 

 data-attributes : you can create custom attributes using this , they must start with "data" like "data-createdby" : "HARI"  in HTML .  .dataset se we can see those . 
 Insertion Methods : tareeka ; let div = document.createElement("div") ; div.innerHTML = "I have been created by <b> Narayan </b>" ; div.setAttribute("class" , "created") ; document.querySekector(".container").append(div) . append se last me insert hota hai . .prepend se ekdm starting me insert krdega , .before se us element se just pehle , .after se us element ke just after , .replaceWith() se us node ko hi replace krdega . 
 Inserting adjacent HTML : ex ; let cont = document.querySelector(".container") ; cont.insertAdjacentHTML (<b> "position" , "Hello Namaste Sha Shi Kaal </b>") , positions : 
 beforeBegin , afterBegin , BeforeEnd , afterEnd , positions ke naam me hi position hai . 
 similarily there are metgjods to insert text , element by insertAdjacenttext/HTML/element . 

.className se element ki classes return krdega . .classList.add/remove("class-name") , 
.classList.toggle("class_name") se toggle ho jayegi . toggle matlab agar hai to remove krdo aur nhi hai to remove krdo . 

Events in JS : there are many events listed on MDN page , check events on mouse click like dblclick
, mousedown (mouse ka button click krne pe) , mouseleave (mouse leke vapis lane pe) etc . 
contextmenu (jab mouse right click kiya jaye)  . similarly many such keyboard events , form events and other events too . 

addEvenetListener function is applied on element which takes two parameters trigger and function to perform . 

Event Bubbling : it is bubbling of event like when you have added an alert event on clicking on an element which is child of some parent element , when user clicks on that element technically user had also clicked on its parent elements as child lies inside parent element . therefore that alert or some other event propogates through out the parents which is called event bubbling and you can stop it by ; example : 
button.addEventListener("click" , (e)=> {
e.stopPropagation()}) , jis element pe ye lgaoge , fir uske repect me event propagate nhi hoga .

EventListener can be removed by using .removeEventListener but handler should be the same . 
event.type se type of event , event.currentTarget se element that handked the event , event.clientX/clientY se coord of mouse . 

events can be handled through html attrubutes too like argument ; onclick = "alert('hello')" type = "button" . or directly by : event.onclick = function yo want to run . 

setIntervel : it is a function whihc takes a function and time (in ms) after which it repeats . 
setInterval(()=> {} , 2000) , it can be used like to change bg-color of an element after a fixed interval using some function and this set interval function . 
other function to look is clearInterval . it is used to cancel setInterval 
setTimeout same syntax , us se utne interval ke baad ek baar repeat hota hai what you want (function) . clearTimeout se vo timeout hatt jata hai . it is used to cancel setTimeout . 

example ; let time = setTimeout(()=>{element.style.bg-color = "red"} , 3000) means 3 sec ke baad bg color red hojayega 
but clearTimeout(time) will cancel it if given command before 3 sec . similarily clearInterval works . 

Sync : it means the instructions are run in a sequence one after another . ,,eans everything is in a sync . 
Async nature of JS : exaple you have written a set of instructions and it include an istruction which will take some time say 2 sec to execute but then instructions after that instruction will face an unnecessary delay , so aysnc nature doesnt wait for that instruction to get executed first but skip it till then it gets executed and then after its execution is over , it displays its output  .  

Callback : callback is a function inside a function kind of recurrsion concept . It is an argument inside another function . example : 
setTimeout(()=> {console.log("hello"),2000}) , here setTimout is taking a function as an argument which is callback , function inside a function . you can even pass an function which will include other function as well .

Pyramid of doom or Callback hell :This is actually nested callback . when we have callback inside callback and so on , code gets very difficult to manage and looks like a pyramid . Promise() is used to handle this thing . 

snippets are shortcuts for your help , you can create snippets in js by giving them a name , prefix to be used , assigning a body to them and cursor positions too . 

Promises is a solution to callback hell . it is a promise of code execution . it takes two paraeters : resolve and reject . new Promise is a keyword which is used as a constructor here . example :
let pro = new Promise(resolve , reject) {console.log("Promise speaking this side")} . Promise has 3 states : pending , fulifilled and rejected . if resolve() is used then state fulfilled , reject() is used then state is reject , if nothing used then pending() . write that variable in console to check the state and other details of promise . resole and rejet itself are callbacks . resolve() se result lete hain and reject se we can display an error . 

now after promise got resolved or rejcted whatever uske baad hume koi kaam krvana hota hai . if promise get resolved or say state becomes fulfilled then we use promise.then() , isme function pass krne pe tabhi run krega agar promise fulfilled or resolved hua to and we use promise.catch() agar promise reject hone pe kuch kaam krana ho . promise.catch ke andar pass kiya gya function tabhi run krega jab promise reject ho gya ho . 

also promise.then() aur promise.catch() me by default success aur error ka message as a paramter passed hota hai , you canp print that too . jaise try and catch me hota tha in python . 

we get promises from API's , api return promise . we use promise.then and promise.catch to use them . 
promise chains : like we created 2 async functions which takes 4 sec to execute and we called them they get executed at the same time after 4 sec but we want func2 after func1 get executed , so we call func2 inside promise of func1.then which acts like a nested promise or a promise chain . 
we dont need to use callback now as promises are doing the same task with better syntax and less complex code . 
chain of .then is refered to promise chain . 

Async await : more better thing is to use Async await . we are studying the methods to handle async functions in JS . callbacks were hell , promises were better and now Async await will be the best approach . 

Async function always return a promise . any function can be turned to an async function by using async keyword .
await keyword pauses the execution of its surrounding async function until the promise is settled . we can't directly use await . we have to make an async function and inside that we can use await keyword for a function . 
basically we have achieved that pehle ye kaam ho tab tak parallel me doosra async func na run ho , uske baad run ho using async and await . await keyword stops every other execution till then that function or command is being executed .

IIFE : to use async and await , we have to create a function to use await keyword , for this we can use this IIFE concept in whihc function gets executed as soon as it is defined . to do this syntax is :
(function ()={}) () which is that you have to keep your function (without any name) in parenthesis , it can be any function : async , arrow etc and then outside that parenthesis you have to other set of parenthesis for execution of this function . so you dont have to seperately call a function . 

try - catch error handling : parseInt() se strings gets converted to Integer if there is a numer in string form , otherwise it gives error as NaN , we have func to check NaN also as isNan() returns boolean . 
throw keyword is used to display errors in some conditions , like throw syntaxError . there are many such errors to throw , refer to MdN . 
syntax for error and catch is ; try { <instruction you wanna try to execute> 
} catch (error) { <instruction you wanna execute if there is an error>} . this wont work for async functions ! 
you can also print err.name , err,message , err,stack etc in catch section .  

finally : instruction which is to be executed either try runs or catch runs . finally block actuall significance is inside a function including try , catch and using return in that case because when you return then other section of code below return clause doesn't run but in case of finally it runs , that's actual use of finally clause . 

to get a random numer in a specific range say for a to b is :
a + (b-a)*Math.random() ab iski range a to b me ayegi . 

Advanced JS concepts : IIFE (immediately invoked function expression) is one concept which is already mentioned above .
next concept is Destructuring : example if you say ; let x,y = [2,5] and then you print value of x , y then it is not gonna assign x , to array items but if you type let [x,y] = [4,8,5] then it will assign x , to array items as 4 , 8 to x , y . 
one more thing if you do like [x,y,...<variable>] = [23,67,84,98,71,12,35] , then x , y = 23 , 67 and variable will hold array of [84,98,71,12,35] . 
in objects : let obj = {a:1 , b:2 , c:3} , let {c,d} = obj then value of c and d will hold value of 1,2 . 
spread operator : spread the array and apply operations to values acc to function . 
if you try to create an object like : const arr = [2,6,3] , const obj = {...arr} , then obj will be like 
{0:2 , 1:6 , 2:3} . 
variable declared outsude a func becomes global and variable declared within the func becomes local . 

Hoisting : refers to the process where interpreter appears to move the declerations to the top of the code before execution . works onlyin case of var ; for ex : 

console.log(r)


var r = 3

here it should display an error because you have defined variable after executing the command but it doesn't displays an error and shows value as undefined which shows that it appear like interpreter has moved the var r to the top just above console.log(r) block without assigning any value . remember it doesn't happens it just appears like that . 

OOPS in JS : advanced JS topic . class is like an empty form and objects of class are filled up forms . 
each object has a prototype which helps in adding properties to the object . 
constructor runs each time an object is created . jo kaam python me self krta tha vo kaam yaha this keyword krta hai . this or self is always used to refer to an object , this directly means object which has been created . class inherit krne ke liye extends keyword is used . new keyword is used to create an object of a class . 
you must use super keyword  with same paramter to be passed  to call constructor of class inherited to use it's properties otherwise there will be an error . 
method overwritting is changin a function for a particular class and its objects . it is less used but good to know . getter and setter , get and set keyword are used , get runs while getting an info and sets run while setting or changing a value or a property . static keyword bhi hai . 
instamceOf : it is used to confirm whether this object belongs to this class returns a boolean . syntax : 
<object instanceOf <class> .
we can set prototype using __proto__ , we use constructor when we want to set some properties to an object as soon it is created . 

fetch api is used to receive as well as send the data to server . on using it , it returns a promise . 
fetch() is used to fetch api and for requests also . its syntax includes .then two times , it returns 2 promises . one for loading data and second for parsing it into a json form . .json converts data in js object form . like await x.json where x is some defined variable . 

there are requests like : get to get data from the server , post to post some data to server , patch is used to update some data , post is used when sensitive data has to be posted , put req to update data on server and delete req to delete some data from server . if there is no other argument passed in fetch() other than url then by default it sends a get request tot the server which is used to get some data . 
headers contain additional info like content of page , status code etc 

some terms related to API fetch : AJAX is async JS and XML where XML is old format of data transfer using network request . JSON is advanced format for data transfer using request by using API . it's format looks similar look a js object . json() method is used to convert that JSON data into java script object data . 2 responses need to be resolved in an API call . first one is resolved by fetch() and second is resolved by using json() . 

There are a lot oflearning in the project of currency converter and Spotify clone . 